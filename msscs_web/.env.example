# MSSCS Web P2P Configuration

# Discovery Server Configuration
PORT=9001
USE_HTTPS=false
HOST=localhost
PUBLIC_URL=ws://localhost:9001

# For production deployment with SSL:
# USE_HTTPS=true
# CERT_PATH=/path/to/cert.pem
# KEY_PATH=/path/to/key.pem
# PUBLIC_URL=wss://discovery.yourdomain.com

# PeerJS Server Configuration (optional - uses cloud by default)
# PEERJS_HOST=localhost
# PEERJS_PORT=9000
# PEERJS_PATH=/peerjs

# For internet-wide connectivity:
# 1. Deploy discovery server on a public VPS/cloud server
# 2. Configure firewall to allow the PORT
# 3. Set PUBLIC_URL to your public domain or IP
# 4. Enable HTTPS for production
# 5. Update client apps to use your PUBLIC_URL
